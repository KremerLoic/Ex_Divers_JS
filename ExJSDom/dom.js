/*var elements = document.querySelectorAll('bleu');
for ( var i = 0; i < elements.length; i++){

	console.log(elements[i].textContent);
}
*/


//////// Modifier du texte de l'html via l'ID CSS 'coucou' ////////////



/*var element = document.querySelector('#coucou');
nouveau_texte = ' Je suis le nouveau texte';

if (element.innerText){
	element.innerText = nouveau_texte;
}
else if (element.textContent){
	element.textContent = nouveau_texte;
}

console.log(element);
*/




//////// Changer dans l'html la classe utilisée. Ici je remplace blue par red /////////

/*var element = document.querySelector('.blue')
element.className = 'red';
*/